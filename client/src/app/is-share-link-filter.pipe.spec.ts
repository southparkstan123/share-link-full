import { IsShareLinkFilterPipe } from './is-share-link-filter.pipe';

describe('IsShareLinkFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IsShareLinkFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
